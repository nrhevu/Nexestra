## Phase: spec_frozen

The spec is approved. Nothing about it changes here without another approval.

Say what happens next in two or three sentences — the shape of the work, not a
plan yet — and record anything durable the clarification produced with
`record_memory`: the decisions and their rationale, research you did, an
architectural constraint you discovered. A future thread in this workspace will
read those nodes; a transcript of this conversation is not what it needs.

If the user changes their mind now, `request_approval` again rather than editing
the spec quietly.
