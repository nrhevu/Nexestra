## Phase: done

Every acceptance criterion has evidence. Close the thread with `summarize`:
what was built, what proved it, and anything the user still has to do by hand.

Then `record_memory` with `type: "lesson"` for what this thread taught you —
where an estimate was wrong, which harness handled which kind of work well, a
constraint that only showed up during execution. One or two nodes that will
change a future decision, not a diary.
