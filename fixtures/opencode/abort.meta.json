{
  "harness": "opencode",
  "harnessVersion": "1.18.25",
  "platform": "darwin arm64 (Darwin 27.0.0)",
  "node": "v24.19.0",
  "recordedAt": "2026-09-02",
  "scenario": "aborted session",
  "serverArgv": [
    "opencode",
    "serve",
    "--port",
    "4791",
    "--hostname",
    "127.0.0.1",
    "--print-logs",
    "--log-level",
    "INFO"
  ],
  "serverCwd": "/WORK/repo",
  "promptRequest": "POST /session/{id}/prompt_async -> 204",
  "abortRequest": "POST /session/{id}/abort -> true (HTTP 200)",
  "abortedAfterMs": 7000,
  "notes": "Abort surfaces as session.error {name:'MessageAbortedError'} then session.status idle + session.idle; the assistant message keeps info.error = MessageAbortedError."
}
