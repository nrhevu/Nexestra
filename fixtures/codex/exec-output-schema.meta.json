{
  "harness": "codex",
  "harnessVersion": "codex-cli 0.148.0",
  "platform": "darwin arm64 (Darwin 27.0.0)",
  "node": "v24.19.0",
  "recordedAt": "2026-09-02",
  "scenario": "edit+test with --output-schema (structured final output) - contains file_change item",
  "argv": [
    "codex",
    "exec",
    "--json",
    "-s",
    "workspace-write",
    "-C",
    "/WORK/codex-b",
    "--skip-git-repo-check",
    "--output-schema",
    "/WORK/out/schema.json",
    "-o",
    "/WORK/out/b-last.md",
    "Add a function add(a, b) to src/math.ts and a test for it in src/math.test.ts, then run 'node --test src/*.test.ts' to verify."
  ],
  "outputSchema": {
    "type": "object",
    "properties": {
      "summary": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": ["ok", "failed"]
      },
      "filesChanged": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "required": ["summary", "status", "filesChanged"],
    "additionalProperties": false
  },
  "stdin": "/dev/null",
  "cwd": "/WORK/codex-b",
  "exitCode": 0,
  "durationMs": 30170,
  "stderr": "Reading additional input from stdin...",
  "notes": "Structured output arrives as the JSON *string* in the final agent_message.item.text and is also written verbatim to the -o file. There is no separate structured field."
}
